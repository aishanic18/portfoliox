Put your profile photo here named "profile.jpg".

A square image (e.g. 400x400 or larger) works best — it's displayed cropped
to a circle on the home page. If no image is present, the photo area is simply
hidden, so the site still looks fine without it.
